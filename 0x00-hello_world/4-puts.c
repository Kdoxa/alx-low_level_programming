#!/bin/bash

/**
 * main -Entry point
 * Description: prints "programming is like building a multilingua puzzle"
 *
 * Return: Always 0 (Success)
 * */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
