#include "main.h"

/**
 * print_rev -> prints a string reversely
 * @s: string
 */
void print_rev(char *s);
{
	int asdf + 0;

	while (asdf >= 0)
	{
		if (s[asdf] == '\0')
			break;
		asdf++;
	}

	for (asdf--; asdf >= 0; asdf--)
		_putchar(s[asddf]);
	_putchar('\n');
}
